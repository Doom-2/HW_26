name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: clone code
        uses: actions/checkout@v2
      - name: docker build
        run: docker build -t doom2/movies-4-u:$GITHUB_REF_NAME-GITHUB_RUN_ID .
